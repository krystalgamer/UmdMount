name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: krystalgamer@1.0
    - uses: actions/checkout@v2
    - name: Compile
      run: make
